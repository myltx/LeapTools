{
  "name": "@leaptools/electron",
  "private": true,
  "main": "dist/main.js",
  "scripts": {
    "dev": "pnpm exec concurrently -n build,run -c yellow,cyan \"pnpm exec tsup --config tsup.config.ts --watch\" \"pnpm exec wait-on dist/main.js http://localhost:3000 && electron .\"",
    "build": "pnpm exec tsup --config tsup.config.ts && pnpm exec electron-builder --config electron-builder.yml",
    "lint": "pnpm exec eslint . --max-warnings=0",
    "typecheck": "pnpm exec tsc -p tsconfig.json --noEmit",
    "clean": "pnpm exec rimraf dist dist-release"
  },
  "dependencies": {
    "get-port": "^7.0.0"
  },
  "devDependencies": {
    "@leaptools/config": "workspace:*",
    "@types/node": "^22.0.0",
    "electron": "^35.0.0",
    "typescript": "^5.6.0"
  }
}
